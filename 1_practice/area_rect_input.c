#include<stdio.h>

	int main()
	{
        int length ,breadth ,area;
         
        printf("enter the length of a rectangle");
        scanf("%d",&length);
        printf("\n enter the breadth");
        scanf("%d",&breadth);

         area=length*breadth;

        printf("area of rectangle is %d",area);

		return 0;

	}

