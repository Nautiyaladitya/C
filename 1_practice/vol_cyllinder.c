#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("\n area of circle is %f", pi * radius * radius);
    int height = 3;
    printf("\n volume of cyllinder is %f", pi * radius * radius * height);

    return 0;
}
